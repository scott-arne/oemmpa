# OEMMPA Documentation Build Dependencies
# Install with: uv pip install -r docs/requirements.txt
# (or run `python -m invoke docs-deps`, which installs these via uv)

# Core Sphinx
sphinx>=7.0

# Theme
sphinx-rtd-theme>=2.0

# Markdown support
myst-parser>=2.0

# C++ documentation
breathe>=4.35
exhale>=0.3

# Python documentation
sphinx-autodoc-typehints>=2.0

# Task and live-reload helpers
invoke>=2.0
sphinx-autobuild>=2024.0
